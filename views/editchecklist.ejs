<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    
    <title>Edit Checklist</title>

    <style>
        body {
            background-image: url('/images/editchecklist.jpg');
            background-size: cover;
            background-repeat: no-repeat;
            background-position: center;
            background-attachment: fixed;
        }
    </style>

</head>

<body>
    <%- include('partials/navbar.ejs') %>

    <div class="container">
        <br>
        <div class="text-center"><h2>Update Checklist</h2></div>
        <br>
        <div class="text-center">
          <% if (checklist) { %> 
            <form action="/editchecklist/<%= checklist.Checklist_ID %>" method="POST" enctype="multipart/form-data">
                <div class="mb-3">
                    <label for="Name" class="form-label">Checklist Name:</label>
                    <input type="text" class="form-control" id="Name" placeholder="Enter checklist name" value = <%= checklist.Name %> name="Name" required>
                </div>

                <div class="mb-3">
                    <label for="Description" class="form-label">Description:</label>
                    <input type="text" class="form-control" id="Description" placeholder="Describe your checklist" value = <%= checklist.Description %> name="Description" required>
                </div>

                <div class="mb-3">
                    <label for="image">Current Image</label>
                    <input type="text" name="currentImage" value = "<%= checklist.image %>" disabled>
                    <img src="images/<%= checklist.image %>" width = "50%">
                </div>

                <div class="mb-3">
                    New Image: <input type="file" id="image" name="image" accept="image/*">
                </div>
                
                <button type="submit" class="btn btn-primary">Update Checklist</button>
            </form>
            <% } else { %>
                <p>No checklist found.</p>
            <% } %>
            <a href ="/viewchecklist">Back</a>
        </div>
    </div>
</div>
</body>
</html>
